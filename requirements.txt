# Core deep learning
torch>=2.2.0
torchvision>=0.17.0

# Data / numerics
numpy>=1.24
Pillow>=9.5
pandas>=2.0

# Training utilities
tqdm>=4.65
tensorboard>=2.14
matplotlib>=3.7

# Evaluation (FID score)
torchmetrics[image]>=1.2
torch-fidelity>=0.3.0
scipy>=1.10

# Interactive demo
gradio>=4.0

# Optional: only needed for download_lfw.py (auto-fetches a real face dataset)
scikit-learn>=1.2

# ---------------------------------------------------------------------------
# GPU users: the command above installs whatever torch build pip resolves by
# default. For a specific CUDA version, install torch/torchvision FIRST from
# https://pytorch.org/get-started/locally/ (pick your CUDA version there),
# then run `pip install -r requirements.txt` for everything else -- pip will
# see torch/torchvision are already satisfied and skip them.
# ---------------------------------------------------------------------------
